import 'package:flutter/material.dart';

class PlaceAddPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('플레이스 등록하기'),
      ),
      body: Text('PlaceAddPage'),
    );
  }
}
